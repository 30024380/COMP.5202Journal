//Purpose: Displays only 10 numbers between 1 - 50
            int finish = 50;

            for(int start = 1; start <= finish; start++)
            {    
                
                if(start % 5 == 0)
                Console.Write("\t" + start);               
            }
            Console.ReadLine();
        }
    }
}
